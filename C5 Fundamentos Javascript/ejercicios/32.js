function deEuroAdolar(euro) {
  // Debes calcular el valor recibido como argumento pasándolo a dólares.
  // Supongamos que 1 euro equivale a 1.20 dólares.
  // Tu código:
  const dolar = 1.20;
  const total = euro * dolar;

  return total;
}

module.exports = deEuroAdolar;
